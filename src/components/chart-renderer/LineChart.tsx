import * as Highcharts from 'highcharts';
import { LineChartProps } from './LineChart.types';

import React, { memo, useEffect } from 'react';

const LineChart = memo<LineChartProps>(({ position, data }) => {


    return <div />;
});

export default LineChart;
